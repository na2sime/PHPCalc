{
  "name": "architecture",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build": "docker compose up --build -d",
    "start": "docker compose up -d",
    "stop": "docker compose down",
    "stan": "docker compose run --rm phpstan analyse src",
    "cs": "docker compose run --rm phpcs --standard=PSR12 src"
  },
  "author": "Nassime ABDIOU",
  "license": "ISC",
  "description": ""
}
